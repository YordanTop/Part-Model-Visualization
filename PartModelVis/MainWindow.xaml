<Window x:Class="PartModelVis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PartModelVis.ViewModels"
        xmlns:views="clr-namespace:PartModelVis.Views" 
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="MainWindow" MinHeight="650" MinWidth="850" WindowState="Maximized">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:ModuleDescriptionViewModel}">
            <views:ModuleDescriptionView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ModuleDisplayerViewModel}">
            <views:ModuleDisplayerView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:InfoSearcherViewModel}">
            <views:InfoSearcherView />
        </DataTemplate>
    </Window.Resources>

    <Grid HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Stretch" Background="#FF272537">

        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Config">
                <MenuItem Header="Visualization configuration" Command="{Binding VisualConfigCommand}"/>
                <MenuItem Header="Module info configuration" Command="{Binding ModuleConfigCommand}"/>
            </MenuItem>
        </Menu>

        <ContentControl Grid.Column="0"
                    Grid.Row="1"
                    Content="{Binding ModuleDescriptionViewModel}"/>

        <ContentControl Grid.Column="0"
                    Grid.Row="2"
                    Content="{Binding ModuleDisplayerViewModel}"/>

        <ContentControl Grid.Column="0"
                    Grid.Row="3"
                    Content="{Binding InfoSearcherViewModel}"/>


    </Grid>
</Window>

